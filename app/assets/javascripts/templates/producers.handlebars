<div class="contentframe">
  {{#each producer in controller}}
    {{#linkTo "producer" producer}}{{producer.name}}{{/linkTo}}
  {{else}}
    You have no producers! Try making some!
  {{/each}}
</div>
