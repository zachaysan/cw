Producer <br>
{{#if model}}
  <table>
  <tr>
  <th>id</th>
  <th>name</th>
  <th>webhooks</th>
  <th>consumers</th>
  <th> </th>
  </tr>
  <tr>
  <th>{{model.id}}</th>
  <th>{{model.name}}</th>
  <th>{{model.webhook_count}}</th>
  <th>{{model.consumer_count}}</th>
  <th><a class="btn btn-danger" {{action destroyRecord}}>delete</a></th>
  </tr>
  </table>
{{#if consumers.length}}
  {{#each consumer in consumers}}
    consumer.name
  {{/each}}
{{else}}
  You have no consumers
{{/if}}
{{else}}
Something went wrong.
{{/if}}
