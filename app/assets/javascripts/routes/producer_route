App.ProducerRoute = Ember.Route.extend({
  setupController: function(controller, model){
    controller.startEditing();
  },
  
  model: function(params){
    
    return App.Producer.find(params.producer_id);
  }
});
